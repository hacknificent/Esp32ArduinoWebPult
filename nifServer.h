//init
    // wifiConnect
    // newErrors
// newRoute(String title, String address, String &html, bool gStyle );
// newRoute("Home", "", homePage, true);
// newRoute("WebPult", "/pult/", pultPage, true);
// newRoute("Off", "/pult/?pBtn="3", pultPage,  true ); // Loop with signed buttons

// newRoute("Command Route", "/command/", "", false );
// newRoute("Off", "/command/3/", "<h1>Off</h1>", false ); // Loop with signed buttons

//loop
//checkRequest
//processRequest
    // getClientRequest
    // clean /gap / sanitize / xxxRequest(String clientRequest)
    // checkRoutes();
    // sendResponse();
